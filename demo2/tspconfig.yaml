emit:
  - "@typespec/openapi3"
  - "@typespec/json-schema"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{output-dir}/schema"
    openapi-versions:
      - 3.0.0
  "@typespec/json-schema":
    emitter-output-dir: "{output-dir}/json-schema"

linter:
  extends:
    - "@typespec/http/all"

